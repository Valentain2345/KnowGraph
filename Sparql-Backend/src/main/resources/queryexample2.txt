PREFIX ex: <http://example.org/Person/> 
 
SELECT (STRAFTER(STR(?person), "http://example.org/Person/") AS ?personLocal) ?name ?weight 
WHERE { 
  ?person ex:name ?name ; 
          ex:weight ?weight . 
} 
ORDER BY DESC(?weight)